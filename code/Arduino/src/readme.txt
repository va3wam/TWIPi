This is where the C++ code loaded onto the ESP32 inside the robot is located.

main.ino is the code to be loaded onto the ESP32 that runs the TWIPi robot
BlinkLED directory has code that blinks the software controlled LED (white) on the TWIPi peripheral board
LCD16x2Test directory has code that prints "HELLO WORLD" on TWIPi's LCD
